<?xml version="1.0" encoding="utf-8"?>

<layout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto">
    <data>
        <variable name="alert" type="lahzouz.com.campusalert.service.model.Alert"/>
        <variable name="callback" type="lahzouz.com.campusalert.view.callback.AlertClickCallback"/>
        <variable name="alertViewModel" type="lahzouz.com.campusalert.viewmodel.AlertViewModel"/>
    </data>

    <android.support.v7.widget.CardView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:onClick="@{() ->  callback.onClick(alert)}"
        android:layout_marginStart="@dimen/item_horizontal_margin"
        android:layout_marginEnd="@dimen/item_horizontal_margin"
        app:cardUseCompatPadding="true">

        <LinearLayout
            android:layout_marginStart="@dimen/item_horizontal_margin"
            android:layout_marginEnd="@dimen/item_horizontal_margin"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:padding="5dp"
            android:orientation="vertical">

            <TextView
                android:id="@+id/name"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:contentDescription="@string/alert_name"
                android:textStyle="bold"
                android:textSize="@dimen/alert_text"
                android:text="@{alert.name}"/>

            <TextView
                android:id="@+id/languages"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:contentDescription="@string/alert_language"
                android:textSize="@dimen/alert_text"
                android:text="@{String.format(@string/languages, alert.language)}"/>

            <TextView
                android:id="@+id/alert_watchers"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:contentDescription="@string/alert_watchers"
                android:textSize="@dimen/alert_text"
                android:text="@{String.format(@string/watchers, alert.watchers)}"/>

            <Button
                android:id="@+id/button_delete"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:onClick="@{() ->  callback.onDeleteButtonClick(alert)}"
                android:text="@string/button_delete"/>

        </LinearLayout>

    </android.support.v7.widget.CardView>
</layout>
